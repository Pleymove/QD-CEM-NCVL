name: Build & publish QGIS plugin

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Zip plugin folder
        run: zip -r cem_ncvl_qgis_plugin.zip cem_ncvl_qgis_plugin -x "*.pyc" "*/__pycache__/*"

      - name: Upload zip to release
        uses: softprops/action-gh-release@v2
        with:
          files: cem_ncvl_qgis_plugin.zip
